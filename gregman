gexist()
	
	syntax

		gexist <filename>

	A function that checks (returns and echos) whether or not a file or directory exists.

	Returns:

		0 if file/dir exists
		1 if file/dir does not exist



grm()

	syntax

		grm <filename>

	A function that removes a file, but is agnostic as to whether it's a file or a directory.



gpath()

	syntax

		gpath <filename>

	A function that echoes the absolute filepath of a file



#Downloads only the autogenerated subtitles file
	sudo youtube-dl --sub-lang en --write-auto-sub --sub-format srt --skip-download
#lists the sbutitle files in the video
	sudo youtube-dl --list-subs 
